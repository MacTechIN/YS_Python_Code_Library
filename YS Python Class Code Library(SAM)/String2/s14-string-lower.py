# 대문자를 소문자로 변환(lower)

# 문자열을 소문자로 변환하여 새로운 문자열을 리턴
# 기존 문자열은 변경되지 않는다.
# 변수 = 문자열.lower()
A = 'HI'
a = A.lower()
print('A=', A)
print('a=', a)

# 소문자를 소문자로 변환하면 동일한 소문자를 리턴
# 새로운 문자열의 복제의 의미
print('소문자(abc)를 lower로 변환하면?', 'abc'.lower())

